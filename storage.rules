rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    
    // Imágenes de productos - Lectura pública, escritura autorizada
    match /productos/{productId}/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && 
        request.resource.size < 5 * 1024 * 1024 && // Max 5MB
        request.resource.contentType.matches('image/.*');
    }
    
    // Tickets/Comprobantes - Lectura pública, escritura autorizada
    match /tickets/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null &&
        request.resource.size < 2 * 1024 * 1024; // Max 2MB
    }
    
    // Facturas/Comprobantes - Lectura y escritura autorizada para usuarios autenticados
    match /facturas/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null &&
        request.resource.size < 5 * 1024 * 1024 && // Max 5MB
        request.resource.contentType.matches('image/.*');
    }
    
    // Bloquear todo lo demás
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
